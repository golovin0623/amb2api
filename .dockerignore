# 构建上下文裁剪 —— 不进镜像、也不参与构建上下文传输/缓存哈希.
# COPY . . 之前 Docker 会把整个上下文打包发给 daemon, 排除大体积/无关文件
# 既加快每次 build, 也避免 .git 等频繁变动的内容让镜像层缓存频繁失效.

# 版本控制与 CI
.git
.gitignore
.github

# Python 运行/构建产物
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/

# 测试与本地运行时数据 (镜像内不需要)
tests/
creds/
*.log
log.txt
uv.lock

# 前端历史/移动端副本 —— 运行时只用 front/control_panel.html 与 front/assets/
front/control_panel.legacy.html

# 文档与脚手架 (不影响运行)
docs/
plan/
issues/
*.md
!README.md

# 编辑器/系统杂项
.idea/
.vscode/
.DS_Store
